name: Codespace Automation

on:
  workflow_dispatch:  # Permite ejecución manual
  schedule:
    - cron: '0 8 * * 1-5'  # Ejecutar días laborables a las 8 AM

jobs:
  codespace-task:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Setup GitHub CLI
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          
      - name: Install GitHub CLI
        run: |
          # Install GitHub CLI
          curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg
          echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null
          sudo apt update
          sudo apt install gh -y
          
      - name: Authenticate GitHub CLI
        run: |
          echo "${{ secrets.PAT }}" | gh auth login --with-token
          
      - name: Listar Codespaces y obtener información
        id: get-codespace-info
        run: |
          echo "Buscando codespace para el repositorio ${{ github.repository }}..."
          
          # Obtener la lista de codespaces con los campos disponibles
          response=$(gh codespace list --json name,repository,state,displayName)
          
          # Mostrar la respuesta para depuración
          echo "Respuesta de codespaces:"
          echo "$response" | jq .
          
          # Extraer el nombre del codespace para este repositorio
          # El campo repository es un string simple, no un objeto
          codespace_name=$(echo "$response" | jq -r ".[] | select(.repository == \"${{ github.repository }}\") | .name")
          codespace_display=$(echo "$response" | jq -r ".[] | select(.repository == \"${{ github.repository }}\") | .displayName")
          codespace_state=$(echo "$response" | jq -r ".[] | select(.repository == \"${{ github.repository }}\") | .state")
          
          if [ "$codespace_name" = "null" ] || [ -z "$codespace_name" ]; then
            echo "Error: No se encontró un codespace para el repositorio ${{ github.repository }}"
            echo "Respuesta completa:"
            echo "$response" | jq .
            exit 1
          fi
          
          echo "Codespace encontrado: $codespace_name"
          echo "Display Name: $codespace_display"
          echo "Estado actual: $codespace_state"
          echo "codespace_name=$codespace_name" >> $GITHUB_OUTPUT
          echo "codespace_display=$codespace_display" >> $GITHUB_OUTPUT
          echo "codespace_state=$codespace_state" >> $GITHUB_OUTPUT

      - name: Iniciar Codespace (si está apagado)
        if: steps.get-codespace-info.outputs.codespace_state != 'Available'
        run: |
          echo "Iniciando codespace: ${{ steps.get-codespace-info.outputs.codespace_name }}"
          gh codespace start -c "${{ steps.get-codespace-info.outputs.codespace_name }}"

      - name: Esperar a que el Codespace esté disponible (si se inició)
        if: steps.get-codespace-info.outputs.codespace_state != 'Available'
        run: |
          echo "Esperando a que el codespace esté disponible (tiempo máximo: 15 minutos)..."
          CODESPACE_NAME="${{ steps.get-codespace-info.outputs.codespace_name }}"
          
          for i in {1..90}; do
            echo "=== Intento $i de 90 ==="
            
            # Verificar estado del codespace
            status=$(gh codespace list --json name,state | jq -r ".[] | select(.name == \"$CODESPACE_NAME\") | .state")
            
            echo "Estado del codespace: $status"
            
            if [ "$status" = "Available" ]; then
              echo "✅ Codespace está disponible después de $((i*10)) segundos"
              break
            fi
            
            if (( i % 10 == 0 )); then
              elapsed_minutes=$((i/6))
              echo "⏳ Tiempo transcurrido: ${elapsed_minutes} minutos"
            fi
            
            sleep 10
          done
          
          if [ "$status" != "Available" ]; then
            echo "❌ Error: Codespace no está disponible después de 15 minutos"
            exit 1
          fi

      - name: Ejecutar comandos en el Codespace
        run: |
          echo "Ejecutando comandos en el codespace..."
          CODESPACE_NAME="${{ steps.get-codespace-info.outputs.codespace_name }}"
          
          # Comando 1: Navegar al directorio y listar contenido
          echo "📂 Ejecutando comando 1: Listar contenido del directorio"
          gh codespace ssh -c "$CODESPACE_NAME" -- "cd /workspaces/GLMCookies && ls -la"
          
          # Comando 2: Verificar versión de Python
          echo "🐍 Ejecutando comando 2: Verificar versión de Python"
          gh codespace ssh -c "$CODESPACE_NAME" -- "python --version"
          
          # Comando 3: Instalar dependencias (si existe requirements.txt)
          echo "📦 Ejecutando comando 3: Instalar dependencias"
          gh codespace ssh -c "$CODESPACE_NAME" -- "cd /workspaces/GLMCookies && pip install -r requirements.txt"

      - name: Detener Codespace
        if: always()
        run: |
          CODESPACE_NAME="${{ steps.get-codespace-info.outputs.codespace_name }}"
          echo "🛑 Deteniendo el codespace..."
          gh codespace stop -c "$CODESPACE_NAME"
