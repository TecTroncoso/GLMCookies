name: Codespace Automation

on:
  workflow_dispatch:  # Permite ejecución manual
  schedule:
    - cron: '0 8 * * 1-5'  # Ejecutar días laborables a las 8 AM

jobs:
  codespace-task:
    runs-on: ubuntu-latest
    steps:
      - name: Listar Codespaces disponibles
        id: list-codespaces
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.PAT }}
          script: |
            try {
              const response = await github.request('GET /user/codespaces');
              console.log('Codespaces disponibles:');
              response.data.codespaces.forEach(codespace => {
                console.log(`- Nombre: ${codespace.name}, Estado: ${codespace.state}, Repositorio: ${codespace.repository.full_name}`);
              });
              
              // Buscar el codespace para este repositorio
              const targetCodespace = response.data.codespaces.find(c => 
                c.repository.full_name === '${{ github.repository }}'
              );
              
              if (targetCodespace) {
                console.log(`\nCodespace encontrado: ${targetCodespace.name}`);
                // Devolver solo el nombre del codespace
                return targetCodespace.name;
              } else {
                console.log('\nNo se encontró un codespace para este repositorio');
                return null;
              }
            } catch (error) {
              console.log('Error listando codespaces: ', error);
              throw error;
            }

      - name: Verificar si se encontró el codespace
        if: steps.list-codespaces.outputs.result == 'null'
        run: |
          echo "Error: No se encontró un codespace para el repositorio ${{ github.repository }}"
          echo "Por favor, verifica que exista un codespace activo para este repositorio"
          exit 1

      - name: Almacenar nombre del codespace
        id: store-codespace-name
        run: |
          CODESPACE_NAME='${{ steps.list-codespaces.outputs.result }}'
          echo "codespace_name=$CODESPACE_NAME" >> $GITHUB_OUTPUT
          echo "Nombre del codespace: $CODESPACE_NAME"

      - name: Iniciar Codespace usando el nombre
        id: start-codespace
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.PAT }}
          script: |
            const codespaceName = "${{ steps.store-codespace-name.outputs.codespace_name }}";
            try {
              console.log(`Iniciando codespace: ${codespaceName}`);
              const response = await github.request(
                'POST /user/codespaces/{codespace_name}/start', {
                  codespace_name: codespaceName
                }
              );
              console.log('Codespace iniciado: ', response.data);
              return response.status;
            } catch (error) {
              console.log('Error iniciando codespace: ', error);
              throw error;
            }

      - name: Esperar a que el Codespace esté disponible
        run: |
          echo "Esperando a que el codespace esté disponible (tiempo máximo: 15 minutos)..."
          CODESPACE_NAME="${{ steps.store-codespace-name.outputs.codespace_name }}"
          CODESPACE_NAME_ENCODED=$(echo "$CODESPACE_NAME" | jq -sRr @uri)
          
          # Aumentar el tiempo de espera a 15 minutos (90 intentos de 10 segundos)
          for i in {1..90}; do
            echo "=== Intento $i de 90 ==="
            echo "Verificando estado del codespace..."
            
            # Obtener la respuesta completa usando el nombre codificado
            response=$(curl -s -H "Authorization: token ${{ secrets.PAT }}" \
            "https://api.github.com/user/codespaces/$CODESPACE_NAME_ENCODED")
            
            # Mostrar la respuesta para depuración (solo los primeros 200 caracteres)
            echo "Respuesta de la API: ${response:0:200}..."
            
            # Extraer el estado usando jq
            status=$(echo "$response" | jq -r '.state // "unknown"')
            
            echo "Estado del codespace: $status"
            
            if [ "$status" = "Available" ]; then
              echo "✅ Codespace está disponible después de $((i*10)) segundos"
              break
            fi
            
            # Mostrar progreso cada 10 intentos (cada ~1.7 minutos)
            if (( i % 10 == 0 )); then
              elapsed_minutes=$((i/6))
              echo "⏳ Tiempo transcurrido: ${elapsed_minutes} minutos"
            fi
            
            echo "Esperando 10 segundos antes del próximo intento..."
            sleep 10
          done
          
          if [ "$status" != "Available" ]; then
            echo "❌ Error: Codespace no está disponible después de 15 minutos"
            echo "Último estado conocido: $status"
            exit 1
          fi

      - name: Obtener detalles del Codespace
        id: codespace-details
        run: |
          CODESPACE_NAME="${{ steps.store-codespace-name.outputs.codespace_name }}"
          CODESPACE_NAME_ENCODED=$(echo "$CODESPACE_NAME" | jq -sRr @uri)
          response=$(curl -s -H "Authorization: token ${{ secrets.PAT }}" \
          "https://api.github.com/user/codespaces/$CODESPACE_NAME_ENCODED")
          
          # Extraer información importante usando jq
          machine_name=$(echo "$response" | jq -r '.machine_display_name // "unknown"')
          created_at=$(echo "$response" | jq -r '.created_at // "unknown"')
          
          echo "machine_name=$machine_name" >> $GITHUB_OUTPUT
          echo "created_at=$created_at" >> $GITHUB_OUTPUT
          echo "Detalles del Codespace:"
          echo "Máquina: $machine_name"
          echo "Creado: $created_at"

      - name: Ejecutar comandos en el Codespace
        run: |
          echo "Ejecutando comandos en el codespace..."
          CODESPACE_NAME="${{ steps.store-codespace-name.outputs.codespace_name }}"
          CODESPACE_NAME_ENCODED=$(echo "$CODESPACE_NAME" | jq -sRr @uri)
          
          # Comando 1: Navegar al directorio y listar contenido
          echo "📂 Ejecutando comando 1: Listar contenido del directorio"
          curl -X POST \
            -H "Authorization: token ${{ secrets.PAT }}" \
            -H "Accept: application/vnd.github.v3+json" \
            -d '{"command":"cd /workspaces/GLMCookies && ls -la", "working_directory":"/workspaces/GLMCookies"}' \
            "https://api.github.com/user/codespaces/$CODESPACE_NAME_ENCODED/commands" || echo "Comando 1 ejecutado"
          
          sleep 5
          
          # Comando 2: Verificar versión de Python
          echo "🐍 Ejecutando comando 2: Verificar versión de Python"
          curl -X POST \
            -H "Authorization: token ${{ secrets.PAT }}" \
            -H "Accept: application/vnd.github.v3+json" \
            -d '{"command":"python --version", "working_directory":"/workspaces/GLMCookies"}' \
            "https://api.github.com/user/codespaces/$CODESPACE_NAME_ENCODED/commands" || echo "Comando 2 ejecutado"
          
          # Comando 3: Instalar dependencias (si existe requirements.txt)
          echo "📦 Ejecutando comando 3: Instalar dependencias"
          curl -X POST \
            -H "Authorization: token ${{ secrets.PAT }}" \
            -H "Accept: application/vnd.github.v3+json" \
            -d '{"command":"pip install -r requirements.txt", "working_directory":"/workspaces/GLMCookies"}' \
            "https://api.github.com/user/codespaces/$CODESPACE_NAME_ENCODED/commands" || echo "Comando 3 ejecutado"

      - name: Detener Codespace
        if: always()
        run: |
          CODESPACE_NAME="${{ steps.store-codespace-name.outputs.codespace_name }}"
          CODESPACE_NAME_ENCODED=$(echo "$CODESPACE_NAME" | jq -sRr @uri)
          echo "🛑 Deteniendo el codespace..."
          curl -X POST \
            -H "Authorization: token ${{ secrets.PAT }}" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/user/codespaces/$CODESPACE_NAME_ENCODED/stop" || echo "Codespace detenido"
